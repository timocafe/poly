# set terminal png transparent nocrop enhanced size 450,320 font "arial,8" 
# set output 'smooth.1.png'
set boxwidth 0.05 absolute
set xzeroaxis
set yzeroaxis
set zzeroaxis
n = 100
min=0.
max=231
width = (max-min)/n
hist(x,width)=width*floor(x/width)+width/2.0
set boxwidth width*0.9
set style data points
x = 0.0
## Last datafile plotted: "random-points"
plot [min:max][0:1]   "data_hw"  u 6 smooth cnormal t 'normalized cumulative Haswell '
replot  "data_sb"  u 6 smooth cnormal t 'normalized cumulative  SandyBridge '
replot  "data_p7"  u 6 smooth cnormal t 'normalized cumulative  Power7 '
replot  "data_p8"  u 6 smooth cnormal t 'normalized cumulative  Power8 '
